[pytest]
# module-b/tests and module-c/tests share basenames (test_unit.py,
# test_pipeline_smoke.py); importlib mode gives each file a unique module name
# so the whole repo can be collected in one run.
addopts = --import-mode=importlib
testpaths = module-a/tests module-b/tests module-c/tests kiosk/tests
